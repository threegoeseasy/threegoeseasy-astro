---
import classNames from "../utils/classNames";
const { sticky = false } = Astro.props;
---

<footer
  class={classNames(
    "container s mx-auto text-center my-4",
    sticky ? "absolute text-3xl  bottom-4" : "drop-shadow-xl"
  )}
>

</footer>