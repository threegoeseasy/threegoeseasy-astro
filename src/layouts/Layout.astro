---
import ThemeToggle from "../components/ThemeToggle";
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="Web magician, creative developer" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>threegoeseasy - ⚛ 🎀 𝒸𝒶𝓇𝒹 𝓌𝑒𝒷 𝓅𝒶𝑔𝑒 🎀 ⚛</title>
  </head>
  <body class="container mx-auto bg-orange-400 dark:bg-indigo-950 text-gray-900 dark:text-orange-400" >
    <div class="flex justify-center text-3xl items-center gap-8 py-6 px-0 w-full">
      <ThemeToggle client:load />
      <h1>threegoeseasy</h1>
      <a href="https://t.me/qualityoriented">☎</a>
    </div>
    
    <slot />
  </body>
</html>
<style is:global>
  @tailwind base;
  @tailwind components;
  @tailwind utilities;

  @font-face {
    font-family: "w95";
    src: url("/w95fa.woff2") format("woff2");
    font-weight: normal;
    font-style: normal;
    font-display: swap;
  }

  iframe {
    /* aspect-ratio: 4/3; */
    width: 100%;
    height: 700px;
  }

  html {
    font-family: "w95", system-ui, monospace;
  }
  body {
    font-family: "w95", system-ui, monospace;
    min-height: 100vh;
    height: 100%;
    width: 100wv;
    cursor: url(/mouse.png), default;
  }

  a {
    cursor: url(/pointer.png), pointer;
  }
  button {
    cursor: url(/pointer.png), pointer !important;
  }
</style>

<script is:inline>
  const theme = (() => {
    if (typeof localStorage !== "undefined" && localStorage.getItem("theme")) {
      return localStorage.getItem("theme");
    }
    if (window.matchMedia("(prefers-color-scheme: dark)").matches) {
      return "dark";
    }
    return "light";
  })();

  if (theme === "light") {
    document.documentElement.classList.remove("dark");
  } else {
    document.documentElement.classList.add("dark");
  }
  window.localStorage.setItem("theme", theme);
</script>
